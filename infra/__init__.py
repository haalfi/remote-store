"""Top-level infra package.

Hosts the docker-compose stack used by tests and benchmarks, the
single-source-of-truth ``.env`` for port and credential defaults, and the
``_settings`` loader that exposes those defaults to Python callers.
"""
