\* TLC config for MC3.tla (Observer dispatch — I1 EventPerCompletedOp).

SPECIFICATION Spec

CONSTANTS
    Ops       <- MCOps
    MaxCalls  <- MCMaxCalls
    op_read   = op_read
    op_write  = op_write
    op_delete = op_delete

CONSTRAINT
    StateConstraint

INVARIANTS
    TypeOK
    EventPerCompletedOp
