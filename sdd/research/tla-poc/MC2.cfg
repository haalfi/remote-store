\* TLC config for MC2.tla (WR-018 proxy forwarding).

SPECIFICATION Spec

CONSTANTS
    Paths     <- MCPaths
    DataSizes <- MCDataSizes
    MaxClock  <- MCMaxClock
    p1 = p1
    p2 = p2

CONSTRAINT
    StateConstraint

INVARIANTS
    TypeOK
    ProxyForwardUnchanged
    PostWriteCacheNotTracked
    EventPerWrite
