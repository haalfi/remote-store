# Configuration

Configuration in `remote-store` is declarative and immutable. You describe your backends and stores as data, and the `Registry` handles the wiring.

::: remote_store.RegistryConfig

---

::: remote_store.BackendConfig

---

::: remote_store.StoreProfile
